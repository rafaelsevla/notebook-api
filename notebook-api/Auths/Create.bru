meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{API_URL}}/auths
  body: json
  auth: inherit
}

body:json {
  {
    "name": "DHH"
  }
}

script:post-response {
  const data = res.getBody()
  
  bru.setEnvVar("JWT_TOKEN", data.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
