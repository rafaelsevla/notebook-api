### 📘 Desenvolvendo REST / RESTful APIs com Ruby on Rails

Repositório criado para acompanhar o curso **Desenvolvendo REST / RESTful APIs com Ruby on Rails**, focado na construção de aplicações **API-only** utilizando Ruby on Rails, seguindo boas práticas de arquitetura REST, versionamento, autenticação, serialização e testes.

Ao longo do curso, são explorados conceitos fundamentais e avançados para o desenvolvimento de APIs robustas, passando por:

- **Rails API-only**: criação de projetos, análise de requisições e respostas HTTP, verbos HTTP, status codes e CRUDs completos.  
- **REST e RESTful**: princípios REST, uso correto de endpoints, recursos e códigos de status.  
- **Renderização de JSON**: associações (`belongs_to`, `has_many`, `has_one`), nested attributes, i18n aplicado a APIs e estruturação de respostas.  
- **Active Model Serializers (AMS)**: serialização de dados, associações, JSON:API, HATEOAS, links, media types e desserialização.  
- **Autenticação**: HTTP Authentication, autenticação baseada em token, JWT e Devise Token Auth.  
- **Versionamento de APIs**: estratégias com query params, headers, URL, subdomínio e uso da gem Versionist.  
- **Paginação e ajustes HTTP**: Accept vs Content-Type, paginação via headers e JSON API.  
- **Tratamento de erros**: erros amigáveis, estruturação de mensagens e padrão JSON API.  
- **Caching**: Cache-Control, ETag e Last-Modified.  
- **Rack**: fundamentos de Rack, Rack Middleware e criação de middlewares customizados.  
- **TDD em APIs Rails**: testes de APIs com RSpec, validação de status HTTP, estrutura de JSON e melhorias em manipulação de hashes.

O objetivo deste repositório é documentar a evolução ao longo do curso e servir como material de referência para o desenvolvimento de APIs RESTful bem estruturadas, escaláveis e testáveis com Ruby on Rails.

[Link do curso](https://www.udemy.com/course/rubyonrails-api/)
[Github do Jackson](https://github.com/jacksonpires)
